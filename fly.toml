app = 'Yuta'
primary_region = 'sin'

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '8000'
  APP_ENV = 'production'
  APP_DEBUG = 'false'
  LOG_CHANNEL = 'stderr'
  FILESYSTEM_DISK = 'r2'
  DB_CONNECTION = 'pgsql'
  DB_PORT = '5432'

[http_service]
  internal_port = 8000
  force_https = true