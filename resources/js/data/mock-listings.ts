import type { Listing } from '@/types/listing';
import rawMockListings from './mock-listings.json';

export const mockListings: Listing[] = rawMockListings as Listing[];

export default mockListings;
